PORT=4000
# HTTP port for the API + static frontend. If this port is busy,
# the server will try the next few ports automatically.

# MongoDB connection (pick one style below).
# Local example:
MONGODB_URI=mongodb+srv://swayam:Swayam%401399@cluster0.xaghhjb.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
DB_NAME=candidate_profile

# If hosting the frontend on a different origin, list it here
# (comma-separated). Use * during local development if needed.
CORS_ORIGIN=http://localhost:4000

# --- Atlas alternatives (choose ONE approach) ---
# 1) Embed credentials in URI (remember to URL-encode special chars in the password):
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net
# DB_NAME=candidate_profile

# 2) Provide credentials separately (easier to manage passwords):
# MONGODB_URI=mongodb+srv://<cluster>.mongodb.net
# DB_NAME=candidate_profile
# MONGODB_USER=<user>
# MONGODB_PASS=<password>
# MONGODB_AUTH_SOURCE=admin
